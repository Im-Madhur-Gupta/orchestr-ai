{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "uvicorn main:app --reload --host 0.0.0.0 --port 6000"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --reload --host 0.0.0.0 --port 6000"
  }
}
